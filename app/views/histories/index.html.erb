
<h1>Toutes les Histoires validées</h1>

 <p>Il y a <%= @histories.count %> histoires disponibles.</p>

<ol> 
<% @histories.each do |histo| %>
<li>
<b><%= link_to histo.title, histo %> (<%= histo.events.count %>)</b>
<% if signed_admin? %> 
  <%= link_to ' Edit', edit_history_path(histo) %>
  <%= link_to ' | Destroy', histo, confirm: 'Are you sure?', method: :delete %></td>
<% end %>
<p><%= histo.description %></p>
</li> 
<% end %>
</ol>



    <table>
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th></th>
      </tr>
    <% @histories.each do |histo| %>
      <tr>
        <td><%= histo.title %></td>
        <td></td>
        <td><%= link_to 'Show', histo %></td>
        <% if signed_admin? %> 
          <td><%= link_to ' | Edit', edit_history_path(histo) %></td>
          <td><%= link_to ' | Destroy', histo, confirm: 'Are you sure?', method: :delete %></td>
        <% end %>

      <% end %>
    </tr>
    </table>
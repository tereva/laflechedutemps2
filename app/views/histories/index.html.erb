<h1>Listing histories</h1>


<%= form_for(@history) do |f| %>
<div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>

  </div>
<%= f.submit "Créer", class: "btn btn-large btn-primary" %>
<% end %>


<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @histories.each do |history| %>
  <tr>
    <td><%= history.title %></td>
    <td><%= history.description %></td>

    <td><%= link_to 'Show', history %></td>
    
    <% if signed_in? %>
    <td><%= link_to 'Edit', edit_history_path(history) %></td>
    <td><%= link_to 'Destroy', history, confirm: 'Are you sure?', method: :delete %></td>
    <% end %>


  </tr>
<% end %>
</table>

<br />

<%= link_to 'New History', new_history_path %>
